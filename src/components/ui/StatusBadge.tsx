/**
 * Re-exported from `../StatusBadge`. The canonical implementation lives there
 * to preserve existing imports across the app; new code should import from
 * `components/ui/StatusBadge` (or the `components/ui` barrel).
 */
export { default } from "../StatusBadge";
