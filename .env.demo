# Demo / staging build mode — committed so VM & CI can enable Demo MFA
# without relying on a gitignored local `.env`.
#
# Usage:
#   npm run build:demo
#   # or: npx vite build --mode demo
#
# Vite loads: .env → .env.local → .env.demo → .env.demo.local

VITE_COMPANY=Netlink
VITE_DEMO_MFA=true
DEMO_MFA=true
APP_ENV=demo
